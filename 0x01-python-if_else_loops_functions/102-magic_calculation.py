#!/usr/bin/python3
def magic_calculation(a, b, c):
    #this programe does exactly the same the bytecode provided by alx
if a < b:
        return (c)
    if c > b:
        return (a + b)
    return (a*b - c)
